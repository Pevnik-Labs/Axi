The Language Syntax
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Syntax==

===Literals===






Number literals are recognized by the regular expression
`````'-'? digit+ ('.' digit+)? ('e' '-'? digit+)?`````

Chr literals are recognized by the regular expression
`````''' (char - ["'\"] | '\' ([""'?\abefnrtv"] | ["Uux"]? (digit | letter)+)) '''`````

Str literals are recognized by the regular expression
`````'"' (char - [""\"] | '\' ([""'?\abefnrtv"] | ["Uux"]? (digit | letter)+))* '"'`````

Hole literals are recognized by the regular expression
`````'?' letter (["'-_"] | digit | letter)*`````

Id literals are recognized by the regular expression
`````(('_' | letter) (["'-_"] | digit | letter)* '.')* (('_' | letter) (["'-_"] | digit | letter)* | ["!"#$%&'*+,-./:;<=>?@\^|~"]+)`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Syntax are the following:
  | ``apply`` | ``assume`` | ``assumption`` | ``axiom``
  | ``by`` | ``case`` | ``cases`` | ``chaining``
  | ``class`` | ``data`` | ``declaration`` | ``exists``
  | ``for`` | ``forall`` | ``in`` | ``ind``
  | ``induction`` | ``instance`` | ``instantiate`` | ``lemma``
  | ``let`` | ``match`` | ``module`` | ``noncomputable``
  | ``of`` | ``open`` | ``proof`` | ``proposition``
  | ``proving`` | ``qed`` | ``record`` | ``require``
  | ``rewrite`` | ``suffices`` | ``theorem`` | ``type``
  | ``unfold`` | ``where`` | ``with`` | ``witness``

The symbols used in Syntax are the following:
  | { | } | ; | =
  | . | , | \ | by-contradiction
  | pick-any | pick-witness | <-- | <-
  | --> | -> | === | <-->
  | @ | :> | <: | :
  | | | & | ( | )
  | such-that | \/ | /\ | ~
  | => |  |  |

===Comments===
Single-line comments begin with #!, //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of Syntax==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Repl// | -> | ``let`` ``{`` //[Dec]// ``}``
  |  |  **|**  | //Exp//
  |  |  **|**  | ``require`` //Str//
  | //[Dec]// | -> | **eps**
  |  |  **|**  | //Dec//
  |  |  **|**  | //Dec// ``;`` //[Dec]//
  | //Dec// | -> | //StructureKwd// //Rule//
  |  |  **|**  | //Rule//
  |  |  **|**  | ``proof`` ``{`` //[ProofStep]// ``}`` ``qed``
  |  |  **|**  | ``declaration`` //Patterns//
  |  |  **|**  | //PipeClause//
  | //StructureKwd// | -> | ``data``
  |  |  **|**  | ``record``
  |  |  **|**  | ``module``
  |  |  **|**  | ``class``
  |  |  **|**  | ``instance``
  |  |  **|**  | ``axiom``
  |  |  **|**  | ``theorem``
  | //Rule// | -> | //Pat// ``by`` ``{`` //[ProofStep]// ``}``
  |  |  **|**  | //Pat// ``proof`` ``{`` //[ProofStep]// ``}`` ``qed``
  |  |  **|**  | //Pat// ``=`` //Exp//
  |  |  **|**  | //Pat// ``where`` ``{`` //[Dec]// ``}``
  |  |  **|**  | //Pat//
  | //[ProofStep]// | -> | **eps**
  |  |  **|**  | //ProofStep//
  |  |  **|**  | //ProofStep// ``;`` //[ProofStep]//
  | //ProofStep// | -> | //ProofDec//
  |  |  **|**  | ``.`` ``{`` //[ProofStep]// ``}``
  |  |  **|**  | ``cases`` //[Exp]//
  |  |  **|**  | ``cases`` //[Exp]// ``with``
  |  |  **|**  | ``induction`` //[Exp]// ``with``
  |  |  **|**  | //PipeClause//
  |  |  **|**  | ``proving`` //Exp//
  |  |  **|**  | ``apply`` //Exp// ``,`` //[Exp]//
  |  |  **|**  | ``instantiate`` //Exp//
  |  |  **|**  | ``\`` //Patterns//
  |  |  **|**  | ``witness`` //ExpOpt//
  |  |  **|**  | //Exp//
  | //ProofDec// | -> | ``assume`` //Patterns//
  |  |  **|**  | ``by-contradiction`` //Pat11// //TypeAnnOpt//
  |  |  **|**  | ``let`` ``{`` //[Dec]// ``}``
  |  |  **|**  | ``lemma`` //Rule//
  |  |  **|**  | ``open`` //Id//
  |  |  **|**  | ``pick-any`` //Patterns//
  |  |  **|**  | ``pick-witness`` //Patterns// ``for`` //Exp//
  |  |  **|**  | ``chaining`` ``{`` //[ChainLink]// ``}``
  |  |  **|**  | ``rewrite`` //[RewriteItem]//
  |  |  **|**  | ``unfold`` //[Pat2]//
  | //[ChainLink]// | -> | **eps**
  |  |  **|**  | //ChainLink//
  |  |  **|**  | //ChainLink// ``;`` //[ChainLink]//
  | //ChainLink// | -> | //ChainOp// //Exp11//
  |  |  **|**  | //ChainOp// //Exp11// ``by`` ``{`` //[ProofStep]// ``}``
  | //ChainOp// | -> | ``<--``
  |  |  **|**  | ``<-``
  |  |  **|**  | ``-->``
  |  |  **|**  | ``->``
  |  |  **|**  | ``=``
  |  |  **|**  | ``===``
  |  |  **|**  | ``<-->``
  |  |  **|**  | //Exp11//
  | //[RewriteItem]// | -> | //RewriteItem//
  |  |  **|**  | //RewriteItem// ``,`` //[RewriteItem]//
  | //RewriteItem// | -> | //DirectionOpt// //Exp//
  | //DirectionOpt// | -> | ``<-``
  |  |  **|**  | ``->``
  |  |  **|**  | **eps**
  | //Patterns// | -> | //[Param]// //TypeAnnOpt//
  | //[Param]// | -> | **eps**
  |  |  **|**  | //Param// //[Param]//
  | //Param// | -> | //Pat11//
  |  |  **|**  | ``@`` //Pat11//
  |  |  **|**  | ``{`` //Pat// ``}``
  | //[Pat2]// | -> | //Pat2//
  |  |  **|**  | //Pat2// ``,`` //[Pat2]//
  | //Pat// | -> | ``noncomputable`` //Pat1//
  |  |  **|**  | ``type`` //Pat1//
  |  |  **|**  | ``proposition`` //Pat1//
  |  |  **|**  | //Pat1//
  | //Pat1// | -> | //Pat2// ``of`` //[Exp1]//
  |  |  **|**  | //Pat2// ``:>`` //[Exp1]//
  |  |  **|**  | //Pat2// ``<:`` //[Exp1]//
  |  |  **|**  | //Pat2//
  | //Pat2// | -> | //Pat3// ``:`` //Exp1//
  |  |  **|**  | //Pat3//
  | //Pat3// | -> | //Pat4// ``|`` //Pat3//
  |  |  **|**  | //Pat4//
  | //Pat4// | -> | //Pat5// ``&`` //Pat4//
  |  |  **|**  | //Pat5//
  | //Pat10// | -> | ``ind`` //Pat11//
  |  |  **|**  | //Pat11// //Param// //[Param]//
  |  |  **|**  | //Pat11//
  | //Pat11// | -> | //Number//
  |  |  **|**  | //Chr//
  |  |  **|**  | //Str//
  |  |  **|**  | //Hole//
  |  |  **|**  | //Id//
  |  |  **|**  | ``record`` ``{`` //[FieldPat]// ``}``
  |  |  **|**  | ``(`` //Pat2// ``,`` //[Pat2]// ``)``
  |  |  **|**  | ``(`` ``)``
  |  |  **|**  | ``(`` ``witness`` //Pat// ``such-that`` //Pat// ``)``
  |  |  **|**  | ``(`` //Pat// ``)``
  | //Pat5// | -> | //Pat6//
  | //Pat6// | -> | //Pat7//
  | //Pat7// | -> | //Pat8//
  | //Pat8// | -> | //Pat9//
  | //Pat9// | -> | //Pat10//
  | //[FieldPat]// | -> | **eps**
  |  |  **|**  | //FieldPat//
  |  |  **|**  | //FieldPat// ``;`` //[FieldPat]//
  | //FieldPat// | -> | //Id//
  |  |  **|**  | //Id// ``=`` //Pat//
  | //TypeAnnOpt// | -> | ``:`` //Exp1//
  |  |  **|**  | **eps**
  | //ExpOpt// | -> | //Exp//
  |  |  **|**  | **eps**
  | //[Exp]// | -> | //Exp//
  |  |  **|**  | //Exp// ``,`` //[Exp]//
  | //[Exp1]// | -> | //Exp1//
  |  |  **|**  | //Exp1// ``&`` //[Exp1]//
  | //Exp// | -> | //Exp1// ``=`` //Exp//
  |  |  **|**  | ``apply`` //Exp//
  |  |  **|**  | //ProofDec// ``in`` //Exp//
  |  |  **|**  | //Exp1//
  | //Exp1// | -> | ``witness`` //[Exp]// ``such-that`` //Exp1//
  |  |  **|**  | //Exp2// ``:`` //Exp1//
  |  |  **|**  | ``\`` //[Param]// ``->`` //Exp1//
  |  |  **|**  | ``proving`` //ExpOpt// ``by`` ``{`` //[ProofStep]// ``}``
  |  |  **|**  | ``suffices`` //Exp// ``by`` ``{`` //[ProofStep]// ``}``
  |  |  **|**  | ``exists`` //Patterns// ``,`` //Exp1//
  |  |  **|**  | ``forall`` //Patterns// ``,`` //Exp1//
  |  |  **|**  | //Exp2// ``->`` //Exp1//
  |  |  **|**  | //Exp2// ``-->`` //Exp1//
  |  |  **|**  | //Exp2//
  | //Exp2// | -> | //Exp3// ``<-->`` //Exp3//
  |  |  **|**  | //Exp3//
  | //Exp3// | -> | //Exp4// ``\/`` //Exp3//
  |  |  **|**  | //Exp4//
  | //Exp4// | -> | //Exp5// ``/\`` //Exp4//
  |  |  **|**  | //Exp5//
  | //Exp5// | -> | ``~`` //Exp5//
  |  |  **|**  | //Exp6// ``===`` //Exp6//
  |  |  **|**  | //Exp6//
  | //Exp10// | -> | //Exp11// //[Arg]//
  |  |  **|**  | //Exp11//
  | //Exp11// | -> | ``@`` //Id//
  |  |  **|**  | //Exp12//
  | //Exp12// | -> | ``\`` //Patterns// ``where`` ``{`` //[Clause]// ``}``
  |  |  **|**  | ``case`` //Exp// ``where`` ``{`` //[Clause]// ``}``
  |  |  **|**  | ``match`` //Exp// ``with`` //[PipeClause]//
  |  |  **|**  | ``instantiate`` //Exp// ``with``
  |  |  **|**  | ``(`` ``)``
  |  |  **|**  | ``(`` //Exp// ``,`` //[Exp]// ``)``
  |  |  **|**  | ``record`` ``{`` //[Dec]// ``}``
  |  |  **|**  | ``record`` //Exp// ``where`` ``{`` //[Dec]// ``}``
  |  |  **|**  | ``assumption``
  |  |  **|**  | //Number//
  |  |  **|**  | //Chr//
  |  |  **|**  | //Str//
  |  |  **|**  | //Hole//
  |  |  **|**  | //Id//
  |  |  **|**  | ``(`` //Exp// ``)``
  | //Exp6// | -> | //Exp7//
  | //Exp7// | -> | //Exp8//
  | //Exp8// | -> | //Exp9//
  | //Exp9// | -> | //Exp10//
  | //[Clause]// | -> | **eps**
  |  |  **|**  | //Clause//
  |  |  **|**  | //Clause// ``;`` //[Clause]//
  | //Clause// | -> | //[Param]// ``->`` //Exp1//
  | //[PipeClause]// | -> | **eps**
  |  |  **|**  | //PipeClause// //[PipeClause]//
  | //PipeClause// | -> | ``|`` //[Pat2]// ``=>`` ``{`` //[ProofStep]// ``}``
  | //[Arg]// | -> | //Arg//
  |  |  **|**  | //Arg// //[Arg]//
  | //Arg// | -> | //Exp12//
  |  |  **|**  | ``@`` //Exp11//



%% File generated by the BNF Converter (bnfc 2.9.5).
